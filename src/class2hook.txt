
// should be replaced by function App()
class App extends React.Component {
  // constructor - class only
  constructor(props) {
    super(props);
    //init state;
    this.state = {
      isLightOn: true
    };
    // constructor should be replaced by const [state, toggleState] = React.useState({ isLightOn: true })
    this.handleClick = this.handleClick.bind(this);
    // no handler declaration
  }
  // event handler shuld be replaced by function handleClick(){ ... }
  handleClick() {
    // should be replaced by calling toggleState( ! state.isLightOn );
    this.setState({
      isLightOn: !this.state.isLightOn,
    });
  }
  // get rid of render
  render() {
    return (
      <button
        type="button"
        onClick={this.handleClick}
      >
        // should be replaced by { state.isLightOn ? 'AAN' : 'UIT'}

        {this.state.isLightOn ? 'AAN' : 'UIT'}
      </button>
    );
  }
}

export default App;